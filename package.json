{
  "name": "kurumsal-ai-egitim-platformu",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev:api": "pnpm --filter @kaep/api dev",
    "db:generate": "pnpm --filter @kaep/db db:generate",
    "db:migrate": "pnpm --filter @kaep/db db:migrate"
  }
}
